var booking = "Book \"now\"!"; //Defining wariable and giving it string value
window.alert(booking)

var aptName = "\"Maria\", see view apartment.";
var aptName = aptName.fontcolor ("red"); //Using fontcolor method on string variable
document.write(aptName)

document.write("Book \"now\"!"+"\"Maria\", see view apartment."); // Concatenate string


var x = 5;    // Declare x, give it the value of 5
var y = x + 4;  // Declare y, give it the value of x + 2 
// Write y to demo:
document.getElementById("demo").innerHTML = y;